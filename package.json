{
  "name": "building-with-the-claude-api",
  "version": "2.0.0",
  "private": true,
  "description": "Modern TypeScript + Python companion to Anthropic's 'Building with the Claude API' Academy course, updated for the 2026 Claude platform.",
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "lesson": "node scripts/run-lesson.mjs",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test --experimental-strip-types \"shared/typescript/**/*.test.ts\" \"scripts/**/*.test.mjs\"",
    "check": "npm run typecheck && npm run format:check && npm test && node scripts/check-repo.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "prettier": "^3.6.2",
    "typescript": "^5.9.3"
  }
}
